<form method="post" action="{{ path('awesome_candidate_delete', {'id': awesome_candidate.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ awesome_candidate.id) }}">
    <button class="btn">Delete</button>
</form>
